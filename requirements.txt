twilio==8.9.0       # For SMS reminders
#sqlite3             # SQLite database (built-in with Python)
#tk                 # Tkinter for GUI (comes with Python)
schedule==1.2.0     # For scheduling tasks
python-dotenv       #for global var


# Note: tkinter is part of the Python standard library.
# If you're using macOS or Homebrew, you may need to install it separately:
# brew install python-tk@3.11
